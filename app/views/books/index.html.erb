<%= show_messages %>

<h1><%= tr :book, "activerecord.models", 2 %></h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th><%= tr :room, "activerecord.attributes.book" %></th>
      <th><%= tr :book_date, "activerecord.attributes.book" %></th>
      <th><%= tr :schedule, "activerecord.attributes.book" %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.room.name %></td>
        <td><%= book.book_date.strftime("%d/%m/%Y") %></td>
        <td><%= book.schedule %></td>
        <td colspan="3" class="table-action">
          <%= link_to fa_icon("eye"), book, class: 'text-primary', title: tr(:show, "actions") %>
          <%= link_to fa_icon("edit"), edit_book_path(book), class: 'text-success', title: tr(:edit, "actions") %>
          <%= link_to fa_icon("trash"), book, method: :delete, data: { confirm: 'Are you sure?' }, class: 'text-danger', title: tr(:destroy, "actions") %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to fa_icon("plus", text: tr('title.book.new')), new_book_path, class: 'btn btn-secondary' %>

